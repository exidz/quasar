[package]
name = "quasar-test-errors"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[lints]
workspace = true

[dependencies]
quasar-core = { path = "../../../core" }

[dev-dependencies]

[features]
default = ["alloc", "debug"]
alloc = ["quasar-core/alloc"]
debug = []
