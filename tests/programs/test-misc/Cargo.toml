[package]
name = "quasar-test-misc"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[lints]
workspace = true

[features]
client = []
debug = []
alloc = ["quasar-core/alloc"]

[dependencies]
quasar-core = { path = "../../../core" }

[dev-dependencies]
